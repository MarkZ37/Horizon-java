server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/horizon?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

    job-store-type: jdbc

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.markz.horizon.entity

#jwt:
#  # 发行者
#  name: MARKZ37
#  # 密钥, 经过Base64加密,去等 可自行替换 默认development
#  base64Secret: ZGV2ZWxvcG1lbnQ
#  #jwt中过期时间设置(s)
#  jwtExpires: 7200
#  # refresh过期时间(day)
#  refreshExpires: 5